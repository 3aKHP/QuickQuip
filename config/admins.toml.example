# 全局管理员注册表 —— 只认 QQ 号，跨群生效，与群内角色无关。
# 复制为 admins.toml（已 gitignore）并填入真实 QQ 号，改动热重载（约 5s 内生效）。
# 能力范围见 docs/admin/：当前等价于群主/群管理的管理员命令权限；
# 后续高危工具（容器内 Shell 等）将仅对全局管理员开放。
global_admins = [
    # "1000000000",
]
