"""杀戮尖塔（Slay the Spire）公式化回复模块。

独立顶层域，承载 STS 相关的梗公式能力。词表与共用配置在子模块顶层，
每个公式实现为 ``formulas/`` 下一个子包，可独立扩展，互不耦合。

当前公式：
- ``formulas.card_le`` ——「xxx了」（卡牌/遗物名 + 了）

后续公式（如「我说xxxx」「假如xxxx」）以兄弟子包形式加入即可。
"""
