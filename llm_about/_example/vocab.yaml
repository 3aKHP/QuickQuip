# ── QuickQuip 分群称呼词表 ──────────────────────────────────────────────
# 复制此文件夹为你的群号（如 123456/），按群编辑 vocab.yaml。
#
# 此文件只需填写本群与全局词表 不同 的条目。
# - 全局回退：../vocab.yaml
# - 合并规则：同名条目以本群为准
#
# 格式与全局一致：
#   标准名: [别名1, 别名2, ...]  # 备注（可选）

# ── 核心成员 ────────────────────────────────────────────────────────────
核心成员:
  # 张三: [三哥, 哈基三]  # 大部分以三字开头的称呼通常指张三

# ── 次核心成员 ──────────────────────────────────────────────────────────
次核心成员:
  # 李四: [小李, 四爷]

# ── 群内黑话 ────────────────────────────────────────────────────────────
部分黑话解析: |
  # 麻了：群内表达无语/无奈的情绪用语。
